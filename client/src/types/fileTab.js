import { FileSystemItem } from "./file";

/**
 * This describes the shape of the FileTabContext object.
 *
 * FileTabContext = {
 *   activeFile: FileSystemItem | null,
 *   setActiveFile: (file: FileSystemItem) => void,
 *   changeActiveFile: (fileId: string) => void
 * }
 */

export {};
